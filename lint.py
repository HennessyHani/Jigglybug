"""

function to count in a sequence

"""
def count(sequence, item):
    """

    this counts

    """
    counter = 0

    for num in sequence:

        if num == item:

            counter += 1
    return counter
