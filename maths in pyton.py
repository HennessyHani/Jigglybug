length = input('Enter a number for length ')
#ask user for a length, must be an integer

width = input('Enter the number for width ')
#same for width

perimeter = int(length) + int(width) * 2
print(perimeter)
#calculate the perimeter whilst converter the length 
#and width to integers

area = int(length) * int(width)
print(area)
#ditto

